= f.input :email
= f.input :role

- if can? :manage, @resource
  = f.input :current_password if @resource.persisted?
  = f.input :password
  = f.input :password_confirmation
  = f.input :role, as: :radio_buttons,
    collection: AdminUser.roles.keys.map{|role| [role.humanize, role]}.sort
