.col-12.col-sm-8.col-lg-6.col-xl-6.mt-4.search-form
  = text_field_tag :name, @name , class: 'mr-sm-2 mb-2 mb-sm-0 flex-fill', placeholder: 'Nome'
  = select_tag :city,
                grouped_options_for_select(@cities, @city),
                class: 'select-input city',
                include_blank: 'Todo o país'
  button#partner_search.primary-cta.flex-fill.mt-2.mt-sm-0 Pesquisar

  javascript:
    $('#partner_search').click(ev => {
      $.get({ url: '/search',
              data: {city: $('#city').val(), name: $('#name').val() } })
    })

    $('#partner_search').click()