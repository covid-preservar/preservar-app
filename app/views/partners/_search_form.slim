.col-12.col-sm-8.col-lg-6.col-xl-6.mt-4.flex-column.flex-sm-row.search-form
  = text_field_tag :name, @name , class: 'mr-sm-2 flex-fill', placeholder: 'Nome'
  = select_tag :city,
                options_from_collection_for_select(@cities, :itself, :itself, params[:city]),
                class: 'select-input flex-fill mt-2 mt-sm-0 mr-sm-2',
                include_blank: 'Todo o país'
  button#partner_search.primary-cta.flex-fill.mt-2.mt-sm-0 Pesquisar

  javascript:
    $('#partner_search').click(ev => {
      $.get({ url: '/search',
              data: {city: $('#city').val(), name: $('#name').val() } })
    })